- C++ 11 introduces a memory model for C++, which means library support for 
         * threading 
		 * atomic operations.
			- Some of the these operations include(but aren't limited to) 
				* atomic loads/stores
				* compare and swap
				* atomic flags
				* promises
				* futures
				* locks
				* condition variables. 
